修改完项目后，要把项目跑起来给用户看。

推送（git push）之前，必须先检查并更新 README.md，把本次代码改动涉及的功能说明、行为变化、使用方式等同步进去，然后把 README 和代码改动一起提交、一起推送。不要只推代码而不更新 README。

> 说明：官网（site/）和安装包会在 push 到 main 后由 GitHub Actions 自动构建并发布，下载链接固定指向 releases/latest，无需手动改；但 README.md 是普通文件，不会自动同步，必须手动维护。如果本次改动也影响官网展示文案/截图，记得一并更新 site/ 目录。
