{
  "name": "profilepilot",
  "version": "0.1.0",
  "description": "A local desktop control desk for managing Chrome profiles and extensions.",
  "main": "dist/main/main.js",
  "scripts": {
    "build": "tsc",
    "start": "npm run build && electron .",
    "dev": "npm run build && electron .",
    "check": "tsc --noEmit"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^25.9.1",
    "electron": "^42.3.3",
    "typescript": "^6.0.3"
  }
}
